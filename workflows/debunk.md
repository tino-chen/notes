---
type: guide
title: Debunk 事实核查技能
tags: [OpenClaw, Agent, 事实核查, Prompt Engineering]
---

# Debunk 事实核查技能

## 这是什么

一个 OpenClaw Agent 技能（Skill），让 AI 对任意内容进行事实核查并生成结构化报告。不仅能查证信息真伪，还能根据社交语境生成不同风格的回复话术——帮你既纠正错误又不伤和气。

核心挑战不是"搜索信息"，而是**输出质量控制**——AI 天然倾向于讨好、模糊、和稀泥。事实核查需要的是"敢下结论、有据可查、态度鲜明"。

## 最终效果

- 输入一个链接/截图/文字，输出一份结构化的核查报告
- 每条论点都有可信度评级、权威来源链接、具体数据对比
- 社交场景下自动生成四种风格的回复话术（高级情商版、直言不讳版等）
- 公众号等反爬网站有专用 Playwright 脚本兜底

### 样例展示

以下是对一篇"阿里 Qwen3.6-Plus 性能直逼 Claude"文章的事实核查结果：

![可信度评级与核查清单](/debunk-example-1.png)

![锐评总结](/debunk-example-2.png)

![社交回复话术（高级情商版）](/debunk-example-3.png)

## 整体架构

```
用户输入（链接/截图/文字）
    │
    ├─ 场景判断：自查型 or 社交回复型
    │
    ▼
步骤 1：获取内容
    ├─ 公众号 URL → Playwright 脚本（反爬兜底）
    ├─ 其他 URL → web_fetch
    ├─ 截图 → OCR
    └─ 文字 → 直接使用
    │
    ▼
步骤 2：提取核心论点（可验证的事实性声明）
    │
    ▼
步骤 3：逐一查证（web_search + web_fetch 交叉验证）
    │
    ▼
步骤 4：生成输出
    ├─ 场景 A：可信度评级 + 核查清单 + 锐评总结
    └─ 场景 B：上述 + 意图推测 + 回复话术（4 种风格）
```

## 实操步骤

### 第一步：安装 Debunk 技能

```bash
openclaw skills install debunk
```

技能安装后位于 `~/.openclaw/skills/fact-checker/`。内部名称为 `debunk`（从 `fact-checker` 重命名而来），但目录名保持 `fact-checker` 不变。

### 第二步：理解两种使用场景

技能会根据用户输入自动判断场景：

| 场景 | 特征 | 输出内容 |
|------|------|----------|
| **A：自查型** | "事实核查一下"、"这个是真的吗" | 可信度评级 + 核查清单 + 锐评总结 |
| **B：社交回复型** | "朋友转发的，帮我查查"、"群里有人发了这个" | 上述 + 意图推测 + 4 种回复话术 |

区别很简单：有没有社交语境。有人分享给你 → 场景 B，自己查 → 场景 A。

### 第三步：了解输出结构

以场景 B（社交回复型）为例，完整输出包含以下部分：

#### 1. 可信度评级

用 5 星制评级，**必须显示满 5 个符号**：

```
☆☆☆☆☆  完全捏造
⭐☆☆☆☆  几乎全假
⭐⭐☆☆☆  大部分不实
⭐⭐⭐☆☆  有事实基础但被夸大
⭐⭐⭐⭐☆  大部分属实，个别措辞有营销成分
⭐⭐⭐⭐⭐  完全属实
```

#### 2. 核查清单

每条论点严格按以下格式输出：

```
**N. "原文引用的论点"**

✅/⚠️/❌/❓ 一句话结论。关键数据对比和实际情况说明。

🔍 来源名称：一句话概括该来源说了什么（`[原文](URL)`）
```

每条论点**必须**有至少一个权威来源链接，不允许无来源的结论。

#### 3. 锐评总结

主观评论，可以毒舌、调侃、嘲讽。但**禁止**重复核查清单里的具体数据——锐评是"读后感"，不是"摘要"。

#### 4. 回复话术（仅场景 B）

默认展示**高级情商版**，核心结构：

| 环节 | 篇幅 | 要求 |
|------|------|------|
| 肯定 | ~50% | 肯定内容中正确的部分、对方的善意 |
| 不留痕迹引导 | ~50% | 用疑问引导/补充信息/邀请验证等技法递出事实 |

**关键原则**：
- ❌ 绝不允许只有肯定没有引导
- ❌ 禁止用"但是""不过"等明显转折词
- ✅ 用"对了""顺便""说到这个"自然过渡

引导技法示例：

```
# 疑问引导
"这个数字我看着有点奇怪，80分说的是哪个版本来着？"

# 补充信息
"对了之前看到另一个测试里 Claude 还是领先两分左右"

# 邀请验证
"这个数据是官方的还是媒体自己算的？回头可以找原报告看看"
```

### 第四步：了解反爬兜底机制

公众号（`mp.weixin.qq.com`）有反爬，`web_fetch` 大概率失败。技能内置了 Playwright 脚本兜底：

```bash
node ~/.openclaw/skills/fact-checker/scripts/fetch-url.js "<url>" --max-chars 15000
```

抓取优先级：`web_fetch` → Playwright 脚本 → 提示用户复制粘贴。

## 技能开源

- **GitHub**：https://github.com/tino-chen/openclaw-skills/tree/main/debunk
- **ClawHub**：`openclaw skills install debunk`

## 踩坑记录

### 1. AI 不敢下结论

最初版本的技能没有规定"必须显示满 5 个符号"的评级格式，AI 经常给出模糊的"有一定可信度"而不是明确的星级。根本原因是 AI 的讨好倾向——怕得罪人、怕判断错。

**解决**：规定固定格式（☆☆☆☆☆ ~ ⭐⭐⭐⭐⭐），用符号强制 AI 做出明确判断。

### 2. 只有肯定没有引导

社交回复场景中，AI 的默认行为是"你说得对，不过..."——要么全是肯定（阿谀奉承），要么用"但是"硬转折（太刻意）。

**解决**：设计"高级情商版"作为默认风格，规定"肯定 50% + 引导 50%"的结构铁律，提供 5 种引导技法表，明确禁止"但是""不过"等转折词。

### 3. 核查清单缺少来源链接

AI 会写"据媒体报道"但不给具体链接，或者给一个搜索结果页而不是原文链接。

**解决**：规定固定格式（🔍 来源名称 + 一句话概括 + `[原文](URL)`），明确要求"不允许无来源的结论"。

### 4. 公众号文章抓取失败

`web_fetch` 对公众号返回空内容或反爬拦截页。

**解决**：内置 Playwright 脚本 `scripts/fetch-url.js` 作为兜底，优先级为 web_fetch → Playwright → 提示用户手动复制。

## 参考资料

- Debunk 技能源码：https://github.com/tino-chen/openclaw-skills/tree/main/debunk
- ClawHub 发布页：https://clawhub.com/skills/debunk
- OpenClaw 文档：https://docs.openclaw.ai
